[project]
name = "fmf-api"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "fastapi[standard]>=0.115.4",
    "findingmodelforge",
]

[tool.uv.sources]
findingmodelforge = { path = "../findingmodelforge" }

[dependency-groups]
dev = [
    "mypy>=1.13.0",
    "ruff>=0.7.2",
]
